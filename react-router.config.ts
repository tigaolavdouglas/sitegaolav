import type { Config } from "@react-router/dev/config";

export default {
  // Server-side rendering is on by default in framework mode.
  // Every route below is rendered on the server and hydrated on the client.
  ssr: true,
} satisfies Config;
